# Scripts

Scripts used to build the laboratory Docker images.

## Active

- `build-web-server.sh`
- `build-db-server.sh`
- `build-browser-gns3vnc.sh`

## Historical

- `archive/build-client-server.sh` — image built during the initial work, but the node was discarded and is not part of the active topology.
